import { GlobalContext } from "./globalType"

export const initGlobalContext: GlobalContext = {
  isLoading: false,
  setIsLoading: () => {},
  loading: false,
  isLogin: false,
  setIsLogin: () => {}
}
